@model PaginationViewModel

@{
    ViewData["Title"] = "Auktioner";
    ViewData["Message"] = "Välj en auktion för att börja buda.";
}

<partial name="_SearchPartialView" model="@(new SearchViewModel())" />
<partial name="_IndexPartialView" model="@((List<AuktionBudViewModel>)Model.Model)" />
<partial name="_PaginationPartialView" model="@Model.GetWithoutModel()"/>

@section Scripts
{
    <partial name="_TooltipPartialView"/>
}
